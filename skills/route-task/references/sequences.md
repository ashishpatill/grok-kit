# Compiled sequences

Source of truth: `grok-kit route-task <bug|feature|investigate|ship|status|hygiene>`.

The CLI prints JSON `{ intent, sticky: false, steps[] }`. After printing, leave `/route-task`.
