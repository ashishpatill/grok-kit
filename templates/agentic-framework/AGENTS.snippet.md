## Stack profile: Agentic framework

- Document harness run/eval entrypoints first; `drive()` in `.cursor/verify/verify.sh` must be that command
- Skills: prefer progressive disclosure; curator is manual (`skill-curator-manual`)
- Memory: hot pin small; long-tail via ICM — no dual writers on the same hot MEMORY file
- Use `/refine-harness` only with human approval
- MCP: ICM global only unless a tool is project-essential
