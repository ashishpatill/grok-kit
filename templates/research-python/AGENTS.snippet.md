## Stack profile: Research / Python

- Env: document `uv`/`conda`/`venv` activation
- Notebooks under `notebooks/` — keep kernels reproducible
- Evals: state the exact command (`pytest`, custom script)
- Prefer `/orchestrate-rlm` for multi-experiment sweeps
- MCP: none by default; HF only when needed (project-scoped)
