# Project MCP snippets

Copy into a project's `.cursor/mcp.json` only when that product is needed.

User-global MCP should stay slim: **ICM** (+ nothing else by default).

See individual JSON files in this folder.
